# RinVulkan scripts

`check_manifest.py` validates the repository-owned ICD manifest template.
`run_cts_profile.py` executes a bounded JSON profile; external Khronos CTS
cases must be declared `SKIP` unless their environment is present.
